# Base URL of the backend API. This is the ONLY backend configuration the
# browser ever sees. The OpenAI key lives exclusively in the server process and
# is never exposed to the client.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
