# Public production API URL, baked into the browser bundle at build time.
# Not a secret. A NEXT_PUBLIC_API_BASE_URL set in the Vercel dashboard wins.
NEXT_PUBLIC_API_BASE_URL=https://hotel-assistant-api.onrender.com
